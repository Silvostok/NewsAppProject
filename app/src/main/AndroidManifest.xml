<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miguelangelrubiocaballero.newsappproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_newsapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListNewsActivity"
            android:label="News"/>
        <activity android:name=".DetailsActivity" />
        <activity android:name=".NewsPaper"
            android:label="Newspapers"/>
        <activity
            android:name=".SendFeedback"
            android:label="@string/title_activity_send_feedback" />
        <activity android:name=".NewsPaperDetailActivity"></activity>
    </application>

</manifest>